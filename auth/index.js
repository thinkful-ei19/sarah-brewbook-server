'use strict';

//import router and strategies and export

// const {router} = require('./router');
// const {localStrategy, jwtStrategy} = require('./strategies');

// module.exports = {router, localStrategy, jwtStrategy};
